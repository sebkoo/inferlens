// InferlensFlags — FeatureFlagProvider plus a local JSON provider (rung 16), and the
// EntitlementProvider seam behind the flag system (rung 24). The place a remote-config
// system drops into later. Depends only on InferlensCore.
