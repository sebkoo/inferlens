// InferlensFlags — FeatureFlagProvider plus a local JSON provider, and the EntitlementProvider seam
// behind the flag system. The place a remote-config system drops into later. Depends only on
// InferlensCore.
//
// Still a skeleton: no protocol, no provider, no cache. Both pieces are their own ladder rungs,
// cited here by name rather than by number — a rung number hand-typed outside docs/ROADMAP.md rots
// silently the moment the ladder moves, which is what these two lines had already done.
