// InferlensStore — the append-only SQLite run ledger with versioned migrations (rung 14)
// and the NoSQL document/KV store for model metadata and the flag cache (rung 15).
// Depends only on InferlensCore.
