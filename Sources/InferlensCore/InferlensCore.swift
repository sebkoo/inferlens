// InferlensCore — the inference contract: InferenceEngine, ModelDescriptor,
// LatencySample, InferenceOutcome. These protocols and value types land at rung 03, and
// the conformance suite every engine must pass at rung 04. Zero dependencies by design
// (docs/adr/0001-module-boundaries.md). This placeholder only makes the target compile.
