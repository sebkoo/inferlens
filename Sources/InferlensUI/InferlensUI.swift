// InferlensUI — SwiftUI views expressing the InferenceState machine (rung 19). No engine
// knowledge: depends only on InferlensCore's value types and the engine protocol, never a
// concrete engine.
