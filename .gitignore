# Build
.build/
.swiftpm/
DerivedData/
xcuserdata/
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/

# Fetched artifacts — NEVER committed (see docs/adr/0002, docs/research/MODEL_PROVENANCE.md)
*.mlmodel
*.mlmodelc
*.mlpackage
*.tflite
*.xcframework
*.xcframework.zip
Vendor/
# The label table is DERIVED from the pinned .mlmodel by make bootstrap (scripts/extract-labels.py)
# and staged as an app resource. Derived, checksum-pinned, and fetched — never committed, exactly
# like the model bytes it comes from.
Sources/InferlensApp/Models/imagenet_labels.txt

# Bench output
bench-results/

# Local-only notes — NEVER committed
NOTES.local.md
.claude/

# The maintainer's signing team (ADR-0011): the app shell's xcconfig reads it via an optional
# include; create with  echo 'DEVELOPMENT_TEAM = <your team id>' > App/Signing.local.xcconfig
App/Signing.local.xcconfig

# macOS
.DS_Store
