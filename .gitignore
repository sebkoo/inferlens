# Build
.build/
.swiftpm/
DerivedData/
xcuserdata/
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/

# Fetched artifacts — NEVER committed (see docs/adr/0002, docs/research/MODEL_PROVENANCE.md)
*.mlmodel
*.mlmodelc
*.mlpackage
*.tflite
*.xcframework
*.xcframework.zip
Vendor/

# Bench output
bench-results/

# Local-only notes — NEVER committed
NOTES.local.md
.claude/

# The maintainer's signing team (ADR-0011): the app shell's xcconfig reads it via an optional
# include; create with  echo 'DEVELOPMENT_TEAM = <your team id>' > App/Signing.local.xcconfig
App/Signing.local.xcconfig

# macOS
.DS_Store
