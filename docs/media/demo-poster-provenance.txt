# demo-poster.png — provenance. Hand-authored, deliberately NOT a line in capture-manifest.txt:
# the render test rewrites that file wholesale (StateScreenshotTests.writeManifest, atomic write),
# so a hand-added line there would be erased at the next regeneration — planted drift. This file
# holds the one image the render test did not draw, and the README caption is written from it
# (ADR-0007, invariant 7).

source: inferlens-shell-demo.mov — the 42.7 s uninterrupted simulator demo take, hosted as a
        release asset at tag demo-sim-ac8d402 (never tracked; ADR-0007 Decision 1). Taps in that
        take were posted by a scripted pointer; one take, no cuts.
frame-at: 16.0 s
device: iPhone 17 Pro simulator
model-identifier: iPhone18,1
os: iOS 26.1
view-code: ac8d402
processing: downscaled 1206x2622 -> 414x900; no crop, no quantization, no retouch, no compositing

files:
  demo-poster.png  414x900 px  232871 bytes

# Supersedes the poster taken from the demo-sim-b1c8fbe take (frame-at 22.5 s, view-code b1c8fbe,
# 221932 bytes). That frame showed "class 973" where this one shows "cliff #973" — the whole
# reason the take was re-recorded (rung 38, ADR-0012).
